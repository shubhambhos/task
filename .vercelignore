# Dependencies (Vercel installs from package.json)
node_modules

# Local env and IDE
.env
.env.*
!.env.example
.idea
.vscode
*.log

# OS
.DS_Store
Thumbs.db

# Optional: exclude from upload (Vercel still runs install + build)
# dist is build output, not needed in upload
dist
